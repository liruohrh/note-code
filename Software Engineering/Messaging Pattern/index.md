- **定义**：消息传递模式定义系统间或组件间的通信方式，关注数据的传递和解耦。
- **特点**：
    - 关注 **如何传递数据**。
    - 提供解耦和异步通信的机制。
- **常见消息传递模式**：
    - **发布-订阅模式**：发布者发送消息，订阅者接收消息。
    - **消息队列模式**：通过队列传递消息，支持异步处理。
    - **请求-响应模式**：客户端发送请求，服务端返回响应。